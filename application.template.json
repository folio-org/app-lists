{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-fqm",
      "version": "^1.2.0-SNAPSHOT",
      "preRelease": "only"
    }
  ],
  "modules": [
    {
      "name": "mod-lists",
        "version": "latest",
        "preRelease": "only"
    }
  ],
  "uiModules": [
    {
      "name": "folio_lists",
        "version": "latest",
        "preRelease": "only"
    }
  ]
}